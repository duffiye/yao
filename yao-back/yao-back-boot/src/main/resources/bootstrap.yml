spring:
  application:
    name: back-server
  cloud:
    nacos:
      config:
        server-addr: nacos.91xft.cn:80
        file-extension: yml
        group: YAO_BACK_GROUP
  profiles:
    active: dev
  main:
    allow-bean-definition-overriding: true

